# GersangTrade TODO 리스트

> **완료 기준**: 코드 구현 + 테스트 작성·통과 = 100%
> **최종 업데이트**: 2026-03-24

---

## 진행 현황 요약

| 구분 | 항목 수 | 평균 완료율 |
|------|---------|------------|
| MVP 기능 | 8개 | ~42% |
| 추가 기능 (확장) | 5개 | ~35% |
| 인프라·배포 | 3개 | ~5% |

---

## 1. MVP 기능

### 1-1. 인증 (OAuth2 Google 로그인)

**완료율: 50%**

| 항목 | 상태 | 비고 |
|------|------|------|
| Google OAuth2 로그인 흐름 | ✅ 구현 | `CustomOAuth2UserService`, `OAuth2LoginSuccessHandler` |
| JWT 발급 (AccessToken 15분 / RefreshToken 7일) | ✅ 구현 | `JwtTokenizer` |
| RefreshToken 재발급 엔드포인트 | ✅ 구현 | `AuthController` |
| 로그아웃 (RefreshToken 무효화) | ✅ 구현 | |
| SecurityConfig (역할별 접근 제어) | ✅ 구현 | |
| 단위 테스트 (AuthService) | ❌ 없음 | |
| 통합 테스트 (OAuth2 흐름) | ❌ 없음 | |

**100% 조건**: JWT 발급·재발급·로그아웃 서비스 단위 테스트, SecurityConfig 접근 제어 통합 테스트 통과

---

### 1-2. 아이템 카탈로그 조회

**완료율: 40%**

| 항목 | 상태 | 비고 |
|------|------|------|
| 아이템 검색 API (`GET /api/items`) | ✅ 구현 | `ItemSearchController` |
| 아이템 검색 서비스 | ✅ 구현 | `ItemSearchService` |
| QueryDSL 동적 검색 (`ItemQueryRepository`) | ✅ 구현 | |
| 주술 목록 조회 (장비별 적용 가능 주술) | ✅ 구현 | `RitualApplicability` 연관 |
| 아이템 검색 단위 테스트 | ❌ 없음 | |
| 주술 조회 단위 테스트 | ❌ 없음 | |

**100% 조건**: 검색 조건별(이름·타입·슬롯) 케이스 + 결과 없음 케이스 테스트 통과

---

### 1-3. 거래 등록글 (판매 등록)

**완료율: 85%**

| 항목 | 상태 | 비고 |
|------|------|------|
| 판매 등록 (`POST /api/listings`) | ✅ 구현 | `ListingService` |
| 목록 조회 (`GET /api/listings`) | ✅ 구현 | QueryDSL |
| 상세 조회 (`GET /api/listings/{id}`) | ✅ 구현 | |
| 취소 (`DELETE /api/listings/{id}`) | ✅ 구현 | softDelete |
| 재료/장비/세트 번들 분기 처리 | ✅ 구현 | |
| 주술 적용 검증 | ✅ 구현 | |
| 단위 테스트 17개 | ✅ 통과 | `ListingServiceTest` |
| 세트 표기 자동 생성 (`풀 XX 00세트` 형식) | ❌ 없음 | 번들라인 조합 로직 미구현 |
| 세트 표기 테스트 | ❌ 없음 | |

**100% 조건**: 세트 표기 문자열 생성 로직 구현 + 5피스 풀세트/부분주술 케이스 테스트 통과

---

### 1-4. 구매 희망 등록글

**완료율: 85%**

| 항목 | 상태 | 비고 |
|------|------|------|
| 구매 희망 등록 (`POST /api/wanted`) | ✅ 구현 | `WantedListingService` |
| 목록 조회 (`GET /api/wanted`) | ✅ 구현 | |
| 상세 조회 (`GET /api/wanted/{id}`) | ✅ 구현 | |
| 취소 (`DELETE /api/wanted/{id}`) | ✅ 구현 | |
| 장비 조건·주술 조건 분기 처리 | ✅ 구현 | |
| 단위 테스트 16개 | ✅ 통과 | `WantedListingServiceTest` |
| 주술 조건 복수 케이스 경계값 테스트 | ❌ 부분 | 추가 케이스 여지 있음 |

**100% 조건**: 강화 범위 경계값(min/max enhance) 검증 테스트 + 리팩터링 후 회귀 테스트 통과

---

### 1-5. 거래 신청·확정 흐름

**완료율: 20%**

| 항목 | 상태 | 비고 |
|------|------|------|
| `TradeApplication` 엔티티 | ✅ 구현 | |
| `TradeConfirmed` 엔티티 | ✅ 구현 | |
| 거래 신청 API (`POST /api/listings/{id}/applications`) | ❌ 없음 | 컨트롤러·서비스 미구현 |
| 신청 수락/거절 API | ❌ 없음 | |
| 거래 확정 API (판매자 확인) | ❌ 없음 | |
| 상태 전이 검증 (`PENDING → ACCEPTED → CONFIRMED`) | ❌ 없음 | |
| 단위 테스트 | ❌ 없음 | |

**100% 조건**: 신청→수락→확정 전체 흐름 + 거절·취소·이중신청 예외 케이스 테스트 통과

---

### 1-6. 신고 시스템

**완료율: 15%**

| 항목 | 상태 | 비고 |
|------|------|------|
| `Report` 엔티티 | ✅ 구현 | |
| 신고 접수 API (`POST /api/reports`) | ❌ 없음 | 컨트롤러·서비스 미구현 |
| 관리자 신고 목록 조회 | ❌ 없음 | |
| 신고 처리·기각 API | ❌ 없음 | |
| 사용자 차단 API (`PATCH /admin/users/{id}/block`) | ❌ 없음 | |
| 단위 테스트 | ❌ 없음 | |

**100% 조건**: 신고 접수, 처리, 기각, 사용자 차단/해제 서비스 테스트 통과

---

### 1-7. 시세 조회 (가격 히스토리)

**완료율: 15%**

| 항목 | 상태 | 비고 |
|------|------|------|
| `TradeStatDaily` / `TradeStatMonthly` 엔티티 | ✅ 구현 | |
| 거래 확정 시 통계 집계 트리거 | ❌ 없음 | 서비스 미구현 |
| 시세 조회 API (`GET /api/items/{id}/price-history`) | ❌ 없음 | 컨트롤러·서비스 미구현 |
| 일별/월별 집계 쿼리 | ❌ 없음 | |
| 단위 테스트 | ❌ 없음 | |

**100% 조건**: 집계 로직 + 기간별 조회 API + 데이터 없음 케이스 테스트 통과

---

### 1-8. 유저 프로필·관리

**완료율: 20%**

| 항목 | 상태 | 비고 |
|------|------|------|
| `User` 엔티티 (소프트딜리트, 차단) | ✅ 구현 | |
| 내 정보 조회 (`GET /api/users/me`) | ❌ 없음 | |
| 내 등록글 목록 (`GET /api/users/me/listings`) | ❌ 없음 | |
| 회원 탈퇴 (`DELETE /api/users/me`) | ❌ 없음 | softDelete |
| 단위 테스트 | ❌ 없음 | |

**100% 조건**: 내 정보 조회, 탈퇴(소프트딜리트), 차단 상태 접근 거부 테스트 통과

---

## 2. 추가 기능 (확장)

### 2-1. 능력치 가성비 비교

**완료율: 15%**

| 항목 | 상태 | 비고 |
|------|------|------|
| `ItemStat` 엔티티 | ✅ 구현 | ELEMENT_VALUE / ELEMENT_PIERCE / RESIST_PIERCE |
| `ValueMetricMonthly` 엔티티 | ✅ 구현 | avgPrice / statValue / valueForMoney |
| 가성비 집계 배치 (월간) | ❌ 없음 | `statValue ÷ avgPrice` |
| 가성비 조회 API (`GET /api/stats/value-for-money`) | ❌ 없음 | 스탯 타입·서버별 필터 + 내림차순 정렬 |
| 단위 테스트 | ❌ 없음 | |

**100% 조건**: 집계 배치 + 조회 API + 스탯 타입별·서버별 필터 테스트 통과

---

### 2-2. 데미지 가성비 계산기

**완료율: 50%**

| 항목 | 상태 | 비고 |
|------|------|------|
| 계산기 API (`POST /api/calculator`) | ✅ 구현 | `CalculatorController` |
| 저항 통과율 공식 | ✅ 구현 | 260 cap 처리 포함 |
| 속성 보정 공식 | ✅ 구현 | clamp(-50, +50) |
| 가성비 점수 공식 | ✅ 구현 | 상승률% ÷ 만골드 |
| 직전 달 기본 가격 조회 (`MaterialPriceHistory`) | ✅ 구현 | |
| recommended 배지 로직 (`> 0` 체크) | ✅ 구현 | review에서 수정됨 |
| 단위 테스트 (공식 검증) | ❌ 없음 | |
| 경계값 테스트 (저항 260, 속성 cap) | ❌ 없음 | |

**100% 조건**: 공식 단위 테스트 (저항 365, 디버프 136 예시값 일치 검증 포함) + 경계값 케이스 통과

---

### 2-3. 크롤링 Job 1 — 마스터 데이터 수집

**완료율: 50%**

| 항목 | 상태 | 비고 |
|------|------|------|
| `ItemListTasklet` (geota 아이템 목록) | ✅ 구현 | |
| `ItemDetailReader/Writer` (gerniverse 상세) | ✅ 구현 | |
| `MercenaryListTasklet` (geota 용병 목록) | ✅ 구현 | |
| `MercenaryDetailReader/Writer` (gerniverse 상세) | ✅ 구현 | |
| S3 이미지 업로드 | ✅ 구현 | `S3Config` |
| 관리자 수동 트리거 (`POST /admin/crawler/master`) | ✅ 구현 | |
| 장비 플래그 (`ritualApplicable`, `hasSlotOption`) | ⚠️ 미완 | 항상 false — review 지적 |
| GemGrade 영문 코드 전환 | ⚠️ 보류 | 크롤링 전 완료 권장 |
| 단위 테스트 (Parser, IqrCalculator) | ❌ 없음 | |
| 통합 테스트 (Mock HTML로 파싱 검증) | ❌ 없음 | |

**100% 조건**: `GerniverseParser` 파싱 단위 테스트 (고정 HTML fixture) + 장비 플래그 반영 + 테스트 통과

---

### 2-4. 크롤링 Job 2 — 가격 수집

**완료율: 50%**

| 항목 | 상태 | 비고 |
|------|------|------|
| `PriceCrawlTasklet` (geota 육의전 가격) | ✅ 구현 | |
| IQR 이상치 제거 (`IqrCalculator`) | ✅ 구현 | |
| `MaterialPriceHistory` UPSERT | ✅ 구현 | |
| 월 스케줄러 (매월 1일 03:00) | ✅ 구현 | |
| 관리자 수동 트리거 (`POST /admin/crawler/price`) | ✅ 구현 | |
| `PriceCrawlTasklet` 트랜잭션 범위 검토 | ⚠️ 보류 | review 지적 |
| 단위 테스트 (`IqrCalculator`) | ❌ 없음 | |
| 단위 테스트 (`PriceCrawlTasklet` 집계 로직) | ❌ 없음 | |

**100% 조건**: IQR 계산 단위 테스트 (샘플 데이터 5개 미만 스킵, 이상치 제거 검증) + 트랜잭션 범위 수정 후 통과

---

### 2-5. 관리자 기능

**완료율: 25%**

| 항목 | 상태 | 비고 |
|------|------|------|
| 크롤러 수동 트리거 API | ✅ 구현 | `CrawlerAdminController` |
| 신고 처리 API | ❌ 없음 | |
| 사용자 차단/해제 API | ❌ 없음 | |
| 등록글 숨김 처리 (`hidden=true`) | ❌ 없음 | |
| 관리자 전용 접근 제어 검증 테스트 | ❌ 없음 | |

**100% 조건**: 관리자 권한 요구 엔드포인트 접근 제어 + 비관리자 403 반환 테스트 통과

---

## 3. 인프라·배포

### 3-1. Flyway 마이그레이션

**완료율: 0%**

| 항목 | 상태 | 비고 |
|------|------|------|
| `V1__init.sql` (전체 스키마) | ❌ 없음 | 현재 `ddl-auto=create` |
| `V2__seed_servers.sql` (서버 13개) | ❌ 없음 | |
| `V3__seed_items.sql` (아이템 기초 데이터) | ❌ 없음 | |
| `V4__seed_rituals.sql` (주술 정의) | ❌ 없음 | |
| Production `ddl-auto=validate` 전환 | ❌ 없음 | |
| Flyway 활성화 (`spring.flyway.enabled=true`) | ❌ 없음 | |

**100% 조건**: 마이그레이션 스크립트 실행 후 `validate` 모드에서 스키마 오류 없이 기동 성공

---

### 3-2. 프론트엔드

**완료율: 0%**

| 항목 | 상태 | 비고 |
|------|------|------|
| 기술 스택 결정 (React/Next.js 등) | ❌ 없음 | `/front/` 디렉토리 비어 있음 |
| 아이템 검색·목록 UI | ❌ 없음 | |
| 판매·구매 희망 등록 폼 | ❌ 없음 | |
| 시세 차트 | ❌ 없음 | |
| 데미지 계산기 UI | ❌ 없음 | |
| OAuth2 Google 로그인 버튼 | ❌ 없음 | |

**100% 조건**: 핵심 UI 구현 + E2E 시나리오 (로그인→등록→조회) 동작 확인

---

### 3-3. 운영 환경 구성

**완료율: 10%**

| 항목 | 상태 | 비고 |
|------|------|------|
| 환경변수 템플릿 (`.env.example`) | ❌ 없음 | DB/OAuth/JWT/S3 키 목록화 |
| AWS EC2 배포 스크립트 | ❌ 없음 | |
| RDS 연결 설정 (`application-prod.yml`) | ❌ 없음 | |
| GitHub Actions CI/CD | ❌ 없음 | |
| Spring Batch 스케줄러 모니터링 | ❌ 없음 | |
| `application.yml` 환경변수 분리 | ⚠️ 부분 | 일부 env var 사용 중 |

**100% 조건**: prod 프로파일로 EC2 배포 성공 + 크롤링 스케줄러 정상 기동 확인

---

## 전체 진행률 한눈에 보기

```
[MVP 기능]
1-1. 인증 (OAuth2)              ████████████░░░░░░░░░░  50%
1-2. 아이템 카탈로그 조회       ████████░░░░░░░░░░░░░░  40%
1-3. 거래 등록글 (판매)         █████████████████░░░░░  85%  ← 테스트 완료
1-4. 구매 희망 등록글           █████████████████░░░░░  85%  ← 테스트 완료
1-5. 거래 신청·확정 흐름        ████░░░░░░░░░░░░░░░░░░  20%
1-6. 신고 시스템                ███░░░░░░░░░░░░░░░░░░░  15%
1-7. 시세 조회                  ███░░░░░░░░░░░░░░░░░░░  15%
1-8. 유저 프로필                ████░░░░░░░░░░░░░░░░░░  20%

[추가 기능]
2-1. 능력치 가성비 비교         ███░░░░░░░░░░░░░░░░░░░  15%
2-2. 데미지 계산기              ██████████░░░░░░░░░░░░  50%
2-3. 크롤링 Job 1 (마스터)      ██████████░░░░░░░░░░░░  50%
2-4. 크롤링 Job 2 (가격)        ██████████░░░░░░░░░░░░  50%
2-5. 관리자 기능                █████░░░░░░░░░░░░░░░░░  25%

[인프라·배포]
3-1. Flyway 마이그레이션        ░░░░░░░░░░░░░░░░░░░░░░   0%
3-2. 프론트엔드                 ░░░░░░░░░░░░░░░░░░░░░░   0%
3-3. 운영 환경 구성             ██░░░░░░░░░░░░░░░░░░░░  10%
```

---

## 다음 우선순위 추천 (MVP 완성 순서)

1. **1-5 거래 신청·확정 흐름** — 핵심 서비스 흐름 없이 나머지가 무의미
2. **1-7 시세 조회 API** — 거래 확정 데이터가 생기면 집계 가능
3. **1-6 신고 시스템** — 서비스 운영 안전망
4. **1-8 유저 프로필** — 로그인 사용자 기본 기능
5. **3-1 Flyway** — 프로덕션 배포 전 필수
6. (이후) 추가기능 순서대로