# Grafana 부팅 시 Prometheus 데이터소스 자동 등록
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus          # 대시보드가 이 고정 uid로 참조
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
